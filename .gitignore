node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
.vercel
db.json
# Local snapshot of db.json — holds real user rows and a password field.
db.json.bak
# Old repository, kept only as a local backup.
.git_backup/
# Scratch diffs from local debugging.
diff.txt
diff_utf8.txt
# Ad-hoc dev/test HTML page not part of the application.
run-cheyu/
# Puppeteer screenshots generated during ad-hoc tests.
screenshot.png
# AI Studio project metadata (not part of the codebase).
metadata.json
assets/.aistudio/
